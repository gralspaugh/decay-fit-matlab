classdef FijiRoiTransientTableTest < matlab.unittest.TestCase
    % FIJIROITRANSIENTTABLETEST - Ensure that table can be operated with

    methods (Test)
        function placeholderTest(testCase)
            % This will always be true
            testCase.verifyTrue(true);
        end
    end
end